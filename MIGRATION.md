# 本地课程目录

当前工作副本：`/Users/zhli/Desktop/course/code-new/data`。

Git remote：`https://github.com/zhihuanli/Experimental-Data-Analysis-Course.git`。
在此目录内运行 Git 命令；网页仍由原 GitHub 仓库发布。

原网站工作副本及 `/Users/zhli/Desktop/course/course-code/data` 均保留，未删除。
后者的讲义、代码、数据及原有结果完整复制在 `materials/` 中。复制检查中，差异仅为 Finder 的 `.DS_Store` 文件。

- `chapt1/`、`chapt2/`、`chapt3/`：修订讲义、对应 notebook 和运行用的相对路径输入。
- `materials/`：原始资料归档，不在其中执行会覆盖数据的生成程序。
- `tools/`：讲义更新和验证脚本。
- `work/`：执行日志及检查过程的产物。

`materials/` 约 29 GB，仅保存在本地，不因迁移自动上传到 GitHub。网页和程序使用的数据副本记录在 `data_manifest.json`；大型数据的发布需单独安排，不能作为普通 Git 文件直接推送。
