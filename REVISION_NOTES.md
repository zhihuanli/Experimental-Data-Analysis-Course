# 第一至第三章整理记录（2026-09-13）

## 目录与范围

- 当前 Git 工作目录为 `/Users/zhli/Desktop/course/code-new/data`，remote 保持原课程仓库。
- 原讲义、代码、数据和结果归档于 `materials/`；原位置保留，未删除。
- 课前编程与 ROOT 入门改用实验方法课程的共用入口。旧地址转到相应资料，不再维护两份入门正文。
- 保留第一至第三章的章节顺序、探测器背景和分析主线；作业整合进对应讲义。
- 第四章及后续章节只增加共用页眉，已核对讲义单元正文未改变。

## 主要修订

- 第一章：统一长度、时间和能量的定义；修正衰减与 TOF 公式、重复时间涨落、作业数据生成及二次写树示例。Time-Walk 用事件直接填充 TProfile，仅以能估计方差的均值点作加权拟合。补充 ADC 量化、pedestal、overflow 的适用说明。
- 第二章：核对 PPAC 输入分支；修正 tracking 初始化、参考轨迹选择、效率分母及误差传播。两种编译工程使用同一物理计算，更新 Makefile、输入输出参数及批处理示例。
- 第三章：修正寻峰对象使用、数组容量说明、相邻条能量求和、刻度偏移符号和跨层 cut。归一化不再随机削减某能区的事件；未得到系数的条标为未刻度。多路径补充保留原思路，并修正残差排序、协方差传播及独立性说明。vector 示例核对实际分支类型，保留原事件编号和空事件，缺失时间记为 NaN。
- 保留原装置与方法示意图，重新运行并更新 105 幅代码输出图，输出紧跟对应代码。HTML 与 notebook 的代码和生成图数量逐单元核对。

## 验证

运行环境：ROOT 6.40.02。通过 ROOT 的 Cling 逐单元执行 C++；独立工程另用 `make` 编译并执行。没有安装或改动 Jupyter kernel。章节中的说明性片段需按上下文并入程序，并非每个 Markdown 代码片段都作为独立程序运行。

在课程根目录使用带有 ROOT、nbformat、BeautifulSoup 和 Pygments 的 Python 环境：

```bash
python3 tools/prepare_data.py
python3 tools/check_lecture_pages.py
python3 tools/verify_reconstruction.py
```

- 24 份运行输入／引用图以 SHA-256 核对原始副本。
- PPAC 两种编译程序均由 739,685 个输入事件得到 232,180 个参考轨迹；逐事件比较 11 个输出分支完全一致。
- vector 两个转换程序都保留 1,926,502 个输入事件，事件编号全量检查；抽样核对条号、能量、NaN 时间和候选配对。
- Time-Walk 示例拟合得到 `WL=39.85±0.19`、`WR=45.49±0.23`，与模拟设定 40、45 相比可用于检查校准效果；位置切片和均值拟合仍是近似方法。
- 页面本地图片和文件链接、作业锚点、共用入门资料目标、notebook 结构及 HTML/notebook 代码输出一致性检查通过。

原始资料及大型 ROOT 文件保留在本地，不随网页、notebook 和示例代码的提交上传。见 `MIGRATION.md`。
